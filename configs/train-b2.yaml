folds: [0, 1, 2, 3, 4]
seeds: [20260922, 20260923, 20260924]
output_dir: ../runs/train-b2

model:
  backbone: nvidia/mit-b2
  pretrained: true
  revision: 3bb39e8739149c3777d0325349b2a6c32c6413db
  decoder_channels: 128

data:
  manifest: ../dataset/train.json
  root: ../dataset
  fold_manifest: ../runs/folds.json
  fold_seed: 20260922
  checksum_file: ../dataset/checksums.json
  tile_size: 256
  query_size: 96
  cache_size: 1
  workers: 4

training:
  epochs: 50
  samples_per_epoch: 2048
  batch_size: 2
  validation_batch_size: 2
  learning_rate: 0.00006
  weight_decay: 0.01
  gradient_accumulation: 4
  gradient_clip_norm: 1.0
  ema_decay: 0.999
  amp: true
  threshold: 0.5
  early_stopping_patience: 8
  early_stopping_min_delta: 0.0001
  max_steps: null
